我将分步为您实现任务管理、媒体管理、生成管理页面的查询和批量删除功能。

### 1. 后端 API 升级 (Server)

*   **任务管理 (`server/routes/tasks.ts`)**:
    *   `GET /results`: 增加 `search` 参数支持，允许按任务名称搜索已完成的任务。
    *   `POST /batch-delete`: 新增批量删除接口，接收 ID 数组。
*   **媒体管理 (`server/routes/images.ts`)**:
    *   `GET /`: 增加 `search` 参数支持，允许按文件名 (`original_name`) 搜索。
    *   `POST /batch-delete`: 新增批量删除接口，接收 ID 数组，同时删除数据库记录和云存储文件。

### 2. 前端 API 客户端封装 (`src/admin/api/client.ts`)

*   更新 `getTasks`, `getImages`, `getResults` 方法，增加 `search` 参数。
*   新增 `batchDeleteTasks`, `batchDeleteImages` 方法。

### 3. 前端页面改造 (Admin Pages)

*   **任务管理 (`src/admin/pages/TaskList.vue`)**:
    *   添加搜索输入框和查询按钮。
    *   表格增加多选框列 (`type="selection"`)。
    *   添加批量删除按钮，仅在有选中项时启用。
    *   对接新的搜索和批量删除 API。
*   **媒体管理 (`src/admin/pages/MediaList.vue`)**:
    *   同上，添加搜索和批量删除 UI 及逻辑。
*   **生成管理 (`src/admin/pages/ResultList.vue`)**:
    *   同上，添加搜索和批量删除 UI 及逻辑。

确认后我将开始执行代码修改。